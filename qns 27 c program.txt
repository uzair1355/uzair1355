// Online C compiler to run C program online
#include <stdio.h>

int main() {
     double num=0.0;
     int i,n;
     printf("enter n\n");
     scanf("%d",&n);
     for(i=1;i<=n;i++){
         num=num + 1.0/(2.0*i);
     } 
     printf("%lf",num);
    return 0;
}